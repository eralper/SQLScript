with cte as (
	select 
		LOG_MESSAGE
	from "SAPABAP1"."/PERSONAS/LOG"
	where LOG_LEVEL = 5
)
SELECT * 
FROM XMLTABLE(
'PERSONAS/CONTEXT' 
PASSING cte.LOG_MESSAGE
COLUMNS 
	USERNAME VARCHAR(30) PATH 'USER', 
	TCODE VARCHAR(200) PATH 'TCODE'
) as XML
